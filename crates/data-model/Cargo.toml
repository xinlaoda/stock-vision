[package]
name = "stock-vision-data-model"
version = "0.1.0"
edition = "2021"
description = "Core data models for Stock Vision"

[dependencies]
serde = { version = "1", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
thiserror = "2"
